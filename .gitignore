/target
**/*.rs.bk
Cargo.lock.bak

# IDE
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Per-user LLM provider configs — may contain API keys.
/sica-settings/
idealist_workspace/*.md
sessions/*.*

# Oversized tool outputs the sub-agent spilled to disk (see agents::spill).
spill/

# model-eval reports — regenerated on every run.
evals/reports/
